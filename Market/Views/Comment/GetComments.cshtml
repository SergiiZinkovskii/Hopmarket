@model IEnumerable<Market.Domain.ViewModel.Comment.CommentViewModel>


<div id="comments-section">
   

    @foreach (var comment in Model)
    {
        <div class="col-md-12">
            <div class="card">
                <div class="card-body">
                    <p class="card-title">@comment.Author</p>
                    <p class="card-text">Опис: @comment.Text ...</p>
                    @if (User.IsInRole("Admin"))
                    {
                        <button class="btn btn-danger delete-comment-btn" data-comment-id="@comment.Id">Видалити коментар</button>
                    }
                </div>
            </div>
        </div>
    }

</div>

<script>
    $(document).ready(function () {
        // Видалення коментаря
        $('.delete-comment-btn').click(function () {
            var commentId = $(this).data('comment-id');

            $.ajax({
                url: '/Comment/Delete',
                type: 'POST',
                data: {
                    id: commentId
                },
                success: function () {
                    // Оновлення списку коментарів
                    loadComments();
                },
                error: function (xhr, status, error) {
                    console.log(error);
                }
            });
        });
    });
</script>
